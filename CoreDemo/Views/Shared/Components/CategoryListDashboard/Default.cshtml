@model List<CategoryandBlogPercent>
<div class="row">
    <div class="col-md-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <h3 class="card-title text-center">Kategoriler</h3>
                <div class="table-responsive">
                    <table class="table">
                        <thead>
                            <tr>
                                <th scope="col"> Kategori Adı </th>
                                <th scope="col"> Açıklaması </th>
                                <th scope="col"> Durum </th>
                                <th scope="col"> Tüm Bloglara Oranı </th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td> @item.CategoryName </td>
                                    <td>@item.CategoryDescription</td>
                                    <td>
                                        @if (!item.CategoryStatus)
                                        {
                                            <p class="badge badge-primary">Aktif</p>
                                        }
                                        else
                                        {
                                            <p class="badge badge-secondary">Pasif</p>
                                        }
                                    </td>
                                    <td>
                                        <b class="text-black"> % @item.BlogPercent</b>
                                        <div class="progress mt-sm-2">
                                            <div class="progress-bar bg-gradient-success" role="progressbar" style="width: @item.BlogPercent%" aria-valuenow="@item.BlogPercent" aria-valuemin="0" aria-valuemax="100"></div>
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>